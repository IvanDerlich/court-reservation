function getAllBookingsAction(dispatch, headers) {
  console.log('Bookings Get Action');
  console.log(headers);
  // sends the request with the headers to the API
  // dispatch the resulting information to the state
}
export default getAllBookingsAction;
