const createCourtAction = (dispatch, name, address, description) => {
  console.log('Create Court Action');
  console.log(name, address, description);
  dispatch({});
};

export default createCourtAction;
